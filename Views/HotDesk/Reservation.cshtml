@model CreateReservationViewModel

@Html.BeginForm("", "", FormMethod.Post)
{
    @Html.DropDownList("Text",
        Model.Workplaces.Select(x => new SelectListItem {
            Text = x.ToString(),
            Value = x.ToString()
        }), new {@class="form-control"})
}